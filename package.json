{
  "scripts": {
    "build": "node_modules/.bin/parcel build index.ts --target node --out-dir dist",
    "server": "yarn build && node dist/index.js --torrent=elias"
  },
  "devDependencies": {
    "@types/yargs": "^12.0.1",
    "parcel-bundler": "^1.10.1",
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "pushover-notifications": "^1.0.0",
    "yargs": "^12.0.2"
  }
}
